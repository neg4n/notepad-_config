name: Busted

on:
  push:
  pull_request:

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run Busted
        uses: lunarmodules/busted@v0
        with:
          args: --pattern=_spec\.lua test
